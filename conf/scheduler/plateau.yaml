first_stage:
  target: torch.optim.lr_scheduler.ReduceLROnPlateau
  factor: 0.1
  patience: 5
  verbose: True
  monitor: ERM_val_loss

second_stage:
  target: torch.optim.lr_scheduler.ReduceLROnPlateau
  factor: 0.1
  patience: 5
  verbose: True
  monitor: val_loss